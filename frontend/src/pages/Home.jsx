import { ArrowRight, BarChart3, CloudSun, ShieldCheck } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useFetch } from '../hooks/useFetch'
import { getPlaces } from '../services/api'
export default function Home(){const {data:places}=useFetch(getPlaces); return <main><section className="hero"><div className="hero-copy"><div className="eyebrow"><span className="pulse-dot"/> Live visitor intelligence</div><h1>Make room for the moments that matter.</h1><p>Know the rhythm of Hyderabad's busiest places before you arrive. PulseCity turns patterns into a calmer, more confident visit.</p><div className="hero-actions"><Link className="button primary" to="/predict">Predict my visit <ArrowRight size={17}/></Link><Link className="text-link" to="/dashboard">Explore live data <ArrowRight size={16}/></Link></div></div><div className="hero-orbit"><div className="orbit-center"><span>Today</span><strong>{places?.length || '25'}</strong><small>places indexed</small></div><div className="orbit-card card-a">CURRENT SIGNAL <b>● Clear</b></div><div className="orbit-card card-b">MODEL ACCURACY <b>99.8%</b></div></div></section><section className="stats"><div><strong>36,500</strong><span>observations analyzed</span></div><div><strong>25</strong><span>places across the city</span></div><div><strong>4</strong><span>crowd levels recognized</span></div></section><section className="section"><div className="section-heading"><div><div className="eyebrow">The PulseCity method</div><h2>Less guessing. More going.</h2></div><p>Historical signals and current context, composed into one clear answer.</p></div><div className="feature-grid"><Feature icon={<BarChart3/>} title="See the signal" text="Understand crowd patterns by place, day and time slot."/><Feature icon={<CloudSun/>} title="Read the conditions" text="Weather and traffic context help shape every estimate."/><Feature icon={<ShieldCheck/>} title="Plan with clarity" text="Get a useful recommendation with honest confidence."/></div></section></main>}
function Feature({icon,title,text}){return <article className="feature"><div className="icon-box">{icon}</div><h3>{title}</h3><p>{text}</p></article>}
